# 首次使用改善驗收

2026-09-10，本機 Codex 內建瀏覽器，http://127.0.0.1:8840/viewer/。
依使用者提供計畫 Task 1–5 施工；原始體驗報告與計畫檔保持原樣。
本紀錄只涵蓋介面與互動，沒有重新驗證模型精度或現場通行安全。

## 完成範圍

| Task | 實作狀態 | 驗收 |
|---|---|---|
| 1 核心布局 | 完成 | 三尺寸無水平溢出；單一控制項；DOM 為條件→結果→3D→進階→比較→證據 |
| 2 互動模式 | 完成 | 預設瀏覽、相機操作、端點編輯分開；實際捲輪／點選／鍵盤驗證 |
| 3 查詢與還原 | 完成 | 實際數值比對、成功回饋、非法輸入、保存比較不隨自訂查詢變動 |
| 4 圖例與預覽 | 完成 | 工具與圖例在畫布外；窄版切換／寬版並排；200% 縮放未測，見下表 |
| 5 回歸與文件 | 完成 | 14 Node tests 通過、JS 語法與 diff 檢查通過；契約及操作指南已更新 |

## 三案例最終版實测

均為「RGB-D · 全部觀測／重建證據／還原」、半徑 0.30 m，標記「實驗預設」。

| 案例 | 預期 | 實際 | 結果 |
|---|---|---|---|
| ReplicaCAD 家具 · 通道開放 | 可通行 | 可通行；找到可連通的自由路徑 | 通過 |
| ReplicaCAD 家具 · 沙發移至通道 | 阻斷 | 阻斷；允許未知區仍無連通路徑 | 通過 |
| ReplicaCAD 家具 · 觀測不足 | 未知 | 未知；目前觀測不足 | 通過 |

## 操作紀錄

| 操作 | 實際結果 | 狀態 |
|---|---|---|
| 462×514 首屏 | 案例入口從舊版距頁首約 1546 px，移至新版 195 px；方法與還原在同區 | 通過 |
| 390×844 | 無頁面橫向溢出，畫布非零；主要操作 44 px，hint 12 px | 通過 |
| 1280×800 | 條件／場景／結果分欄；畫布非零、頁面無橫向溢出 | 通過 |
| Tab 從案例選單 | 到條件說明 summary，再依 DOM 進入方法／來源等；不跳到下方研究欄 | 通過（局部走訪） |
| 預設瀏覽點畫布 | S/G 均不變 | 通過 |
| 瀏覽模式畫布捲輪 | 頁面 scrollY 772→1029，端點不變 | 通過 |
| 相機模式畫布捲輪 | 頁面 scrollY 維持 772，重繪計數 7→8，端點不變 | 通過 |
| 完成操作／Esc | 返回 browse；touch-action 為 auto | 通過 |
| 設定 G，方向鍵右移，再 Esc／右移 | G.x 6.75→6.85，退出後仍 6.85；S 不变 | 通過 |
| 設定 S，單次點地面 | 僅 S 更新，回到 browse，標成自訂查詢 | 通過 |
| 編輯模式拖曳約 58 px | S.x 保持 1.25，不觸發端點放置 | 通過 |
| 半徑改 0.40→切方法→切案例→還原 | 前兩次切換保留 0.40；還原回 0.30，摘要同步，成功訊息出現 | 通過 |
| 清空起點 X | 顯示「待完成輸入／待輸入」；沒有轉成 0 | 通過 |
| 修改起點 1.25→1.35 | 自訂查詢；保存比較表文字完全不變 | 通過 |
| RGB-D F0 開啟→深度→Esc→再開 | 感測深度 224 px；關閉後焦點回 F0；再開預設 RGB | 通過 |
| DA3 F0 深度 | 標題明列 DA3、使用預測深度，影像 224 px | 通過 |
| 桌面影格預覽 | RGB／深度同時可見，窄版切換鈕隱藏 | 通過 |
| 標註參考切換 | 摘要明示判定仍用重建網格；回重建後來源恢復 | 通過 |
| 檢視 DA3 分歧 | 正常案例仍阻斷；相同影格配對、非真值誤差提示保留 | 通過 |
| 200% 瀏覽器縮放 | 嘗試 Ctrl+Plus，innerWidth／DPR 均未變，未確認縮放成功 | 未測 |
| 真實手機觸控、讀屏、完整鍵盤走訪 | 沒有相應裝置／測試環境，不能從滑鼠與局部 Tab 推定 | 未測 |
| 人工製造缺圖／網路中斷 | 本輪未改寫 artifacts 或攔截網路；既有 missing 分支保留 | 未測 |

截圖位於本次任務的 Computer Use 工具紀錄，未另外寫入 artifacts：
「改版前：462×514 首屏」、「改版後窄視窗：案例控制先出現」、
「390×844 可讀性與控制尺寸確認」、「1280×800 桌面布局與並排預覽」、
「驗證深度載入、Esc 焦點返回及重新開啟預設 RGB」。
舊版入口不可見也由實際 DOM 距離記錄支持；截圖不是完整可用性研究。

## 程式與資料邊界

- 新增 interaction 純函式與 3 項測試；先觀察缺少模組／helper 的預期失敗，再實作。
  最終執行 planner/rendering/diagnostics/interaction 共 14 項，全數通過。
- 獨立只讀審查未發現重大互動、查詢保存或證據邊界問題。
- viewer/planning.js、rendering.js、diagnostics.js 雜湊與進場前一致。
  app.js 的判定搜尋保留原分支與演算法，只更換主句並另存技術原因。
- study SHA256 保持
  `e368c46e4eda63f21eb17214b95a3f6b66ace2117b91a723b6b63d8b2895a4c2`。
  沒有重跑模型、升級依賴、改寫研究資料或重新製作 artifacts 來源 ZIP。
- 使用者提供的兩份文件雜湊保持原樣。此次修改留在本機供檢閱；未提交、推送或部署。

## 字級與空間後續調整（2026-09-10）

依使用者回饋，viewer 全部內容文字改為至少 16 px，取代前輪較小字級門檻。
涵蓋提示、表格、圖例、影格標籤、預覽及頁尾；面板間距與外邊距縮減，
場景高度不再隨長側欄拉伸，條件說明集中至可展開區。影格標籤移至圖片外。

本輪使用瀏覽器重新載入後量測：

- 1280×800：文件寬度 1265 px，沒有頁面橫向溢出；有版面矩形且含直接文字
  的元素中，computed font-size 小於 16 px 的數量為 0。
- 390×844：文件寬度 375 px，相同字級檢查為 0；首屏與控制無截字。
- 手機影格 F0：對話框寬 337 px，對話框可見元素字級均至少 16 px；
  RGB／感測深度切換入口、關閉鈕與圖片說明可讀。
- 截圖保存於本次任務的瀏覽器工具紀錄；未改寫研究 artifacts。
- 本輪只改 CSS、HTML 說明位置及文件，沒有改動通行演算法或重跑模型。

## 第二輪 R2-01～03 實作驗收（2026-09-10）

依更新後的第二輪報告施工，未重做第一輪功能。原始回饋與計畫保持不變。

- R2-01：新增設定按鈕專用入口；只在畫布未完整可見時 scrollIntoView，
  焦點移至 viewport 並 preventScroll。一般模式切換、還原及 Esc 不加入捲動。
- R2-02：僅保留單一模式操作提示；三態圖例及實虛線含義保持展開，
  點雲說明移入原生「圖層說明」。保留至少 16 px 字級。
- R2-03：相同配對判斷供應圖層旁的可聚焦說明入口；沒有基線、端點
  不完整及診斷失敗分別顯示原因；可用時顯示實際方法配對及非真值誤差。

### 本輪實测

瀏覽器原有縮放造成 viewport 設定值與 innerWidth/innerHeight 不同，以下
以實際 DOM 內容視窗尺寸為準；截圖在本次工具紀錄，縮圖不可作字級量尺。

| 操作 | 實際結果 | 狀態 |
|---|---|---|
| 462×514：從欄位按 G | 畫布 top 77.85 / bottom 436.60，焦點 viewport，座標不變 | 通過 |
| G 方向鍵、Esc、再次方向鍵 | G.x 6.75→6.85；Esc 後維持6.85，S不變，scroll不變 | 通過 |
| G 點畫布中央，再 S 點中央 | 各自只更新選定端點至(3.70,2.70)，成功後回 browse | 通過 |
| 1280×1100 畫布原本完整可見，按 G | scrollY 維持0，焦點 viewport | 通過 |
| 同按鈕再次按下 | 退出 browse；工具自動捲到按鈕，未能獨立驗證點擊本身不跳頁 | 部分驗證 |
| 極短 289×321 進 G | 畫布部分可見、地面可見；Esc 可退出 | 通過 |
| 全部 RGB-D 圖層說明 | difference 停用，明示沒有可配對基線 | 通過 |
| 切 DA3 全部觀測 | 可用，配對 RGB-D 全部→DA3 全部；未自動勾差異 | 通過 |
| 差異、點雲開關及 camera/Esc | 皆可操作，camera 顯示單一動作與退出提示 | 通過 |
| 可見文字與工具 | 小於16px直接文字元素0；按鈕CSS44px，縮放後rect約43.994px | 通過 |
| 三案例：家具通道開放／沙發移至通道／觀測不足 | RGB-D全部、重建、還原、r0.30：可通行／阻斷／未知 | 通過 |
| Node planner/rendering/diagnostics/interaction | 14 passed / 0 failed；JS語法與diff檢查通過 | 通過 |
| 真實觸控、200%縮放、完整鍵盤／讀屏、故意觸發診斷失敗 | 本輪未做 | 未測 |

未重跑模型、重裝依賴、修改研究資料或 artifacts；修改留在本機。

## 交付後鍵盤補測（2026-09-10）

在既有內建瀏覽器實際內容視窗 471×524 操作；這是局部情境的鍵盤驗證，
不等於完整無障礙或讀屏稽核。沒有修改程式、資料或模型。

- 以案例選單為起点，連續 Tab 通過條件展開、方法、來源、還原、判定依據、
  viewport、相機／視角／點雲、圖層說明、半徑、S/G、四座標、影格數。
  以上元素可見焦點 outline 為 solid；停用的差異 checkbox 不接受 Tab，
  但圖層說明 summary 可接受焦點。
- 繼續通過 F0～F23、五個方法比較按鈕、DA3診斷、兩個研究報告入口、
  完整方法量測及資料身分，再回到案例選單。報告僅驗證入口可聚焦，未
  重新驗證外部報告頁的完整鍵盤操作。
- 在 F3 按 Enter 開啟影格，初始焦點 closeFrameDialog；Tab 依序到
  RGB、深度，再經瀏覽器焦點循環回關閉鈕；Esc 後對話框關閉，焦點回F3。
- 在條件說明 summary 按 Enter 可展開／收合。以 Tab 到設定 S，再按
  Enter，焦點移到 viewport；右鍵讓 S.x 1.25→1.35；Esc後再右鍵維持
  1.35，mode為browse。此處右鍵指鍵盤右方向鍵，非滑鼠右鍵。
- 本輪沒有發現需修改程式的鍵盤阻礙；未測真實觸控、讀屏、200%縮放、
  所有方法與異常狀態的完整鍵盤組合。
